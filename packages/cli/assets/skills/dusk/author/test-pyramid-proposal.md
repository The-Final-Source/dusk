---
name: test-pyramid-proposal
---

# test-pyramid-proposal

Propose `<intent>/unit-tests`, `<intent>/integration-tests`, and `<intent>/e2e-tests` children whose triples assert what each pyramid layer must cover, scaled to the parent intent's domain.
