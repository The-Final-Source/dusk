---
name: vitest-invocation
---

# vitest-invocation

Invoke Vitest scoped to a discovered file list (`vitest run <files>`), parse per-test results, and report pass/fail/duration per `@intent-test`. Never run the whole suite when a scoped list is available.
