// @dusk/fixtures-worked-example — the RFC App. B `sendNotification` standing
// regression fixture (P5-T10). Re-exports the canonical loaders; the regression
// test legs live alongside in this package.
export {
  loadWorkedExample,
  workedExampleIntents,
  cleanSourcePath,
  defectsSourcePath,
  WORKED_EXAMPLE_FILE,
  type WorkedExample,
} from "@dusk/fixtures";
