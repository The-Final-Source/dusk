// @intent security/no-inline-credentials [no-inline-credentials]
export function connect(): string {
  // @intent security/no-inline-credentials [no-inline-credentials]
  const apiToken = "fixture-placeholder-token-1234"; // SEEDED: verification/negative-inline-credentials
  // @intent security/no-inline-credentials [no-inline-credentials]
  return apiToken;
}
