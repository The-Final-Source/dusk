---
name: decorator-aware-merge
---

# decorator-aware-merge

Merge decorated code so each statement keeps exactly its winning-side decoration footprint; a merged region must still pass the PreToolUse gate. Escalate semantic conflicts rather than guessing.
