apiVersion: v1
kind: Service
metadata:
  name: dusk-ui-service
spec:
  type: ClusterIP
  selector:
    app: dusk-ui
  ports:
    - port: 80
      targetPort: 3000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dusk-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dusk-ui
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: dusk-ui
    spec:
      containers:
        - name: dusk-ui
          image: <IMAGE_NAME>
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
      terminationGracePeriodSeconds: 30
