{
  "name": "dusk",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:reset": "docker compose down -v && rm -rf ./data/postgres && docker compose up -d",
    "docker:init": "docker compose up -d && sleep 2 && pnpm db:generate && pnpm db:migrate",
    "build": "turbo build",
    "db:generate": "turbo db:generate --filter=@dusk/api",
    "db:migrate": "turbo db:migrate --filter=@dusk/api",
    "db:studio": "pnpm --filter @dusk/api exec drizzle-kit studio",
    "mobile:dev": "pnpm --filter @dusk/mobile dev",
    "mobile:ios": "pnpm --filter @dusk/mobile ios",
    "mobile:prebuild": "pnpm --filter @dusk/mobile prebuild",
    "mobile:build": "pnpm --filter @dusk/mobile build:ios",
    "mobile:build:dev": "pnpm --filter @dusk/mobile build:dev",
    "test": "VITEST_MAX_FORKS=2 VITEST_MAX_THREADS=2 turbo test --concurrency=4",
    "test:correctness": "DUSK_RUN_CORRECTNESS=1 VITEST_MAX_FORKS=2 VITEST_MAX_THREADS=2 turbo test --concurrency=4 --force",
    "typecheck": "turbo typecheck",
    "db:seed": "turbo db:seed --filter=@dusk/api",
    "template:init": "./scripts/init.sh",
    "template:update": "git fetch template && git merge template/main"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@9.15.0"
}
