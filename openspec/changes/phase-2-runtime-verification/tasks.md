> Ordered by dependency (test-harness extension is the leaf; tasks reference only earlier-numbered tasks). Each task names its acceptance (capability spec scenario + plan slug P2-T*) and a Vitest test plan. Control-flow tasks (spawn / memory / compaction / asymmetry) run **zero-model** against the scripted-verdict Verifier double; verdict-correctness tasks (polarity inversion, verdict split, scoped reading, quantifier, compose, prompt structure, worked example) run against the **real frontier model at temperature 0** with the pre-registered protocol `N=3, threshold ≥2/3`. Scaffolding/wiring is unmarked (no behavioral test required).

## 1. Scaffolding & test-harness extension

- [ ] 1.1 Scaffold the new packages per `.praxis/features/adding-packages.md`: `packages/runtime/{orchestrator,memory,skills,tool-scope,verifier,verifier-test-double}`, `packages/delivery/mcp-server`. Each carries `type:module`, `private:true`, `exports`, `composite:true`, tsconfig references, colocated Vitest config; wire Turbo `^build`. Acceptance: `pnpm build`/`typecheck` pass; dep graph respects core→delivery→runtime (wiring — no behavioral test).
- [ ] 1.2 Introduce the `spawnMode: "production" | "test" | "benchmark"` flag (read by injection from `env.ts`, never `process.env` directly) plus the redaction allowlist (D2). Production startup banner logs the mode; `dusk doctor` refuses pipeline operations when `spawnMode !== "production"` (this guardrail ships now; pipeline calls in Phase 3 use it). Acceptance: production-mode traces never carry `raw_prompt` (asserted by 2.5); Test: redaction allowlist unit-tests against a known-secret corpus (Anthropic-key shape + abs-path shape) (unit-only — pure transform).
- [ ] 1.3 Extend `@dusk/test-harness` with: (a) a trace-stream tail reader returning typed `SubAgentTrace[]` for in-test assertions, (b) `raw_prompt` substring/identifier matchers, (c) a `VerifierFixtureScript` type + a `scriptedVerdictFactory(fixtureScript)` shim that the `verifier-test-double` package will implement (the shim is the public seam Phase 3 tests import — it stays test-only). Wiring/extension only — no behavioral test (the seam is exercised by 4.1/4.2).

## 2. sub-agent-runtime  (spec: sub-agent-runtime)

- [ ] 2.1 Define the spawn payload shape, the `VerifierFactory` interface, and the `SubAgentTrace` Zod schema (App. A.6 field set per design D1) in `packages/runtime/orchestrator`. Types via `z.infer`; named exports only. (Scaffolding for 2.2–2.5.)
- [ ] 2.2 Implement `spawnSubAgent({role, beadId?, dialogId?, sessionId, input, verifierFactory?})` as the three-stage deterministic assembler — memory load → skills inject → system-prompt assembly — followed by the Claude Code Task call with `subagent_type: dusk-<role>`. The assembled prompt MUST be the verbatim string passed to Task. Acceptance: spec *Spawn pipeline assembles role + memory + skills before the Task call* + scenarios *Spawning the Engineer materializes its declared skills and bead memory* / *Spawning a Verifier materializes none memory and no diagnosis*; Test (scripted-verdict double, zero-model): drive an Engineer spawn against a fixture role declaring three skills and a populated bead memory → assert assembled prompt includes role body + all three skill bodies + memory rendering; drive a Verifier spawn against the same bead → assert Verifier prompt contains zero substrings from the bead's `## Current diagnosis` (sets up 3.5 / P2-T3).
- [ ] 2.3 Implement `dusk_role_version` range enforcement before any Task call. Acceptance: **P2-T19** + spec *`dusk_role_version` is enforced at spawn time* / scenario *Out-of-range version is refused*; Test (zero-model): out-of-range role file → spawn refused; no Task call recorded.
- [ ] 2.4 Implement `SubAgentTrace` emission to `.ia/observability/traces.jsonl` carrying `schema_version: 1`, `trace_id`, `role`, `invocation_site`, `model`, token counts, latency, `cost_usd`, `skills_loaded[]`, `iteration_number?`; `index_snapshot_id?` is reserved/unset in Phase 2. Acceptance: **P2-T4** + spec *Every spawn emits a SubAgentTrace with the Phase-2 field set* / scenario *A successful Engineer spawn writes a complete trace*; Test (scripted double): Engineer spawn → exactly one trace line written with `skills_loaded[]` matching the role frontmatter exactly.
- [ ] 2.5 Implement `raw_prompt` capture (test/benchmark mode only) with the redaction pass from 1.2 applied to **every** outgoing trace field. Acceptance: spec *`raw_prompt` is captured in test/benchmark mode only, with redaction* / scenarios *Test-mode exposes raw_prompt; production-mode omits it* / *Known-shape secrets are redacted*; Test: same spawn under `spawnMode: "test"` then `"production"` → first trace's `raw_prompt` equals assembled prompt (modulo redaction); second trace lacks the field; redaction integration over an injected fake-key prompt.
- [ ] 2.6 Implement advisory tool/skill scoping (RFC §9.4/§9.7) in `packages/runtime/skills` + `packages/runtime/tool-scope`: only the skills listed in the role's frontmatter are injected; tools list passed to Task as configuration only (no hard sandbox). Acceptance: spec *Tool and skill scoping are advisory in v1* / scenario *A skill outside the role's frontmatter is not injected*; Test: role declaring one skill but with extra files in `.claude/skills/dusk/<role>/` → assembled prompt includes only the declared skill; `skills_loaded[]` matches.
- [ ] 2.7 Author the nine role files under `.claude/agents/dusk-{root,bead,decomposer,scout,engineer,verifier,test-runner,author,conflict-resolver}.md` with v9 frontmatter (`dusk_role_version: 2`, `memory`, advisory `tools`, `skills`, `model`) and complete role-body content per RFC §9.5. The `dusk-verifier.md` role file MUST ship the complete prompt template: a two-path execution structure (deterministic-antecedent path + consequent-only path), an explicit affirmative-framing contract (no "invert if negated" branch), and **2 positive + 2 negative few-shot examples drawn from RFC App. B**. Acceptance: spec *Nine role files exist with v9 frontmatter and complete bodies* / scenario *Verifier role file ships the complete two-path template*; Test: role-file content check — frontmatter parse + presence of named sections + ≥4 few-shot blocks in `dusk-verifier.md`.
- [ ] 2.8 Author the **tier-1 skills** complete: `dusk/verifier/{triple-evaluation,code-span-scoping,polarity-aware-evaluation,implies-evaluation}`, `dusk/engineer/{decoration-completeness,statement-extraction}`, `dusk/author/{polarity-decision,implies-antecedent-grammar}`. Author tier-2 skills at baseline. Acceptance: each skill loads through the spawn pipeline (covered by 2.2/2.6); wiring — content quality is a Sprint-9-dogfood iteration.

## 3. memory-materialization  (spec: memory-materialization)

- [ ] 3.1 Implement the four-scope materializer (`none`/`bead`/`dialog`/`session`) reading and writing under `.ia/runtime/{beads/<bead-id>,dialogs/<dialog-id>,session}/<role>.md`. Missing files render the empty block (never an error). Acceptance: spec *Four memory scopes are materialized at spawn time* / scenarios *First bead spawn finds no memory file* / *Verifier memory: none ignores any existing bead memory file*; Test (zero-model): fresh-bead spawn → empty rendering, no file created until write-back; session-scoped Root spawn → session file created on first write, read on second spawn (sets up spec *Dialog and session memory follow the same structured-file convention* scenario).
- [ ] 3.2 Implement the structured bead memory file format (D6): YAML frontmatter (`bead_id`, `role`, `last_iter`, `last_compacted_at_iter`) + six named Markdown sections per RFC §9.6.1. Parse via a typed API; canonical serialization round-trips byte-identically. Acceptance: spec *Bead memory uses a structured dual-channel format* / scenarios *Populated bead memory parses + re-serializes byte-identically* / *Bead in test-authoring mode populates the test-authoring channel*; Test: round-trip a populated fixture (frontmatter + all six sections) → bytes equal; record an attempt with structural-approach label `mock-call-order` in test-authoring mode → appears under `Approaches tried (test-authoring)` only.
- [ ] 3.3 Implement `compact(memory: BeadMemory): BeadMemory` as a pure deterministic transform (D6) — fires when `## Verifier signals (last 3)` exceeds three entries; keeps the most recent three verbatim; folds older into the appropriate `Approaches tried` channel preserving `(triple_id, focal_verdict, slot_focus, approach_label)`; drops verbose rationale. The transform invokes zero model calls (asserted via trace stream). Acceptance: **P2-T2** (compaction half) + spec *Compaction is a pure, deterministic transform that performs no model call* / scenario *Compaction preserves load-bearing facts and drops rationale*; Test (unit-only for the pure transform + zero-model integration for write-back): 5→3 compaction preserves load-bearing fields, drops rationale text; trace tail confirms no `role: engineer`/`verifier` model call during the write-back.
- [ ] 3.4 Drive the 20-iteration size-bound test via the scripted-verdict double: simulate a 20-iter short cycle (the Bead Orchestrator's iteration loop is Phase-3 surface, but the per-iteration memory write-back path lives here and can be exercised through a Phase-2 test harness directly). Acceptance: **P2-T2** (size-bound half) + spec *Memory size is bounded across iteration depth* / scenario *20-iter simulated drive holds file size near the iter-3 baseline*; Test (zero-model): drive 20 iterations writing back each → assert `size(iter 20) ≈ size(iter 3)` within ±15%.
- [ ] 3.5 Verify the diagnosis-routing structural guarantee through the materializer: the `memory: none` rendering MUST return the empty block regardless of the bead memory file's state (covered structurally by the spawn pipeline's per-role memory selection in 2.2). Acceptance: **P2-T3** + spec *The Engineer's convergence diagnosis is structurally invisible to the Verifier* / scenario *A seeded diagnosis does not appear in the Verifier's raw_prompt*; Test (zero-model): seed `## Current diagnosis` with a distinctive sentinel string, spawn the Verifier against the same bead → assert Verifier trace's `raw_prompt` contains zero substrings of the sentinel; no diagnosis field on the Verifier trace; `convergence_diagnosis_present` only on the Bead-Orchestrator trace.

## 4. verifier-test-double  (spec: verifier-test-double)

- [ ] 4.1 Implement the scripted-verdict factory (`packages/runtime/verifier-test-double`) implementing `VerifierFactory`. Accepts a `VerifierFixtureScript` (typed array of pre-baked `Verdict`s or a `(spawnContext) → Verdict` selector); returns Verdicts in order from successive invocations; performs zero LLM calls; emits the same `SubAgentTrace` shape as the real Verifier with `prompt_tokens: 0`, `completion_tokens: 0`, `cost_usd: 0`. Acceptance: spec *Scripted-verdict Verifier double implements the same factory interface* / scenario *A scripted script returns its verdicts in order with zero LLM cost*; Test: three-Verdict script + three Verifier spawns through `spawnSubAgent` → ordered returns; zero tokens/cost across all three traces.
- [ ] 4.2 Wire factory-injection into `spawnSubAgent` (`verifierFactory?` parameter from 2.1). When provided, the spawn pipeline uses it for Verifier spawns; otherwise defaults to the real Verifier. The selection MUST NOT alter the trace emission path. Acceptance: spec *The double is selected via factory injection at the spawn boundary* / scenario *Same trace shape across real and doubled Verifier spawns*; Test: identical Verifier spawn once with real factory and once with the double → trace field sets match (same keys / type-matching values), differing only on token/latency/cost and verdict content.
- [ ] 4.3 Expose process-local `spawnCount` accessor that increments on every Verifier spawn routed through the double. Acceptance: spec *The double exposes spawn-counter telemetry* / scenarios *spawnCount advances on every Verifier spawn* / *spawnCount does not advance when antecedent-false short-circuits*; Test: 5 spawns → counter reads 5; once 5.1 lands, an antecedent-false `implies` evaluation does not advance the counter (this is the load-bearing assertion Phase 3 control-flow tests will bind to).
- [ ] 4.4 Implement honest exhaustion error: when the script is exhausted, return a typed `DuskError { kind: "internal_error", recoverable: false }` indicating script underrun — never an unscripted Verdict. Acceptance: spec *The double honors fixture exhaustion and selector hints honestly* / scenario *An exhausted script returns a structural error*; Test: 2-Verdict script + 3rd spawn → typed error returned; no fabricated default verdict.

## 5. verifier-procedure  (spec: verifier-procedure)

- [ ] 5.1 Implement `packages/runtime/verifier/antecedent.ts` (D4) — the deterministic antecedent evaluator for `compose: implies`. Inputs: `(intent, unitUnderEvaluation, indexQuery)`. Supports the three closed predicates (`"is decorated with"`, `"claims any aspect of"`, `"is enclosed by a decoration of"`) by index lookup; supports `polarity: negative` antecedent triples as a set-complement query; emits zero LLM calls. Acceptance: spec *Deterministic antecedent evaluation for compose: implies — no LLM call when antecedent false* + spec *All three closed antecedent predicates evaluate by index lookup* + spec *A negative-polarity antecedent triple is a set-complement query* / scenarios listed in those specs; **P2-T6** + **P2-T6b** + **P2-T6c**; Test: per-predicate evaluation against an App.B-like fixture in both directions; assert `spawnCount` from the Verifier double does not advance during the antecedent phase.
- [ ] 5.2 Implement antecedent ambiguity handling: when the `subject` binds to multiple code units the call returns `DuskError { kind: "verifier_evidence_too_large" }` without any LLM fallback. Acceptance: spec *Ambiguous antecedent is a structural error, never an LLM fallback* / scenario *Ambiguous antecedent returns a structural error and never an LLM call*; **P2-T7b**; Test: ambiguous-subject fixture → typed error returned; trace tail records zero LLM calls in the antecedent phase.
- [ ] 5.3 Implement scoped evidence assembly (D8): for `(intent, aspect)` query the focal+support index and read source lines per claimant; bound total assembled lines by `dusk.config.yml > verifier_evidence_max_lines` (default 200); return `DuskError { kind: "verifier_evidence_too_large" }` on overflow without silent truncation. Acceptance: spec *Scoped reading reads focal + named-support evidence only* / scenarios *Verifier input contains only the aspect's claimant lines* / *Evidence overflow returns a structural error*; **P2-T10**; Test (zero-model with raw_prompt assertion): verify `notifications/send [publish-sync-per-insert]` on App. B → `raw_prompt` contains publish line + loop/timestamp/event-payload supports; does NOT contain opt-out, push-dispatch, error-handling lines; oversized-scope fixture returns the typed error and never reaches the LLM.
- [ ] 5.4 Implement the affirmative prompt builder: poses every triple as `"In this code, does <subject> <predicate> <object> hold?"` regardless of `polarity`; annotates with `quantifier` bound + `scope`; for `compose: implies` antecedent-true cases presents **consequents only** (structural property of the assembled payload). Acceptance: spec *Polarity inversion ... the LLM is never asked a negated question* (raw_prompt substring-absence part) + spec *For `compose: implies`, the Verifier prompt presents only consequents*; **P2-T5** raw_prompt half + **P2-T17**; Test (zero-model via double + raw_prompt assertion): 2×2 polarity fixtures → `raw_prompt` has no `does NOT` substring; antecedent-true `implies` raw_prompt contains the consequent triples and not the antecedent triples.
- [ ] 5.5 Implement runtime polarity inversion at the procedure boundary (D7): after the LLM verdict, `focal_verdict = polarity === "negative" ? invert(llmVerdict) : llmVerdict`; preserve original `polarity` in the Verdict for post-hoc attribution; constituent negation inside subject/object NPs does NOT cause re-framing. Acceptance: **P2-T5** inversion half + spec *Polarity inversion happens at the runtime boundary* / scenarios *Polarity inversion truth table (2×2)* / *Constituent negation inside an NP does not reframe the triple*; Test (**real frontier model**, `temperature: 0`, `N=3, threshold ≥2/3`): 2×2 truth-table fixtures; constituent-negation fixture preserves polarity-positive verdict semantics.
- [ ] 5.6 Implement per-support `triple_verdict` extraction (matches | mismatch | vague) — the Verifier prompt asks a structured sub-question per support claim; each claim gets its own verdict. Acceptance: spec *Per-triple verdict splits focal correctness from support quality* / scenarios *Mismatching support does not fail the focal claim* / *A real focal defect fails regardless of pristine supports* / *Vague supports surface as triple_verdict from the model*; **P2-T8** (real-model part); Test (**real frontier model**, `N=3, threshold ≥2/3`): three fixtures — mismatch-support / pristine-supports-real-defect / vague-support — verify the structural outcomes.
- [ ] 5.7 Implement `support_quality` as a pure aggregation rule: `any "mismatch" → "low_confidence"`; else `(vague_count / total) ≥ 0.5 → "low_confidence"`; else `"ok"`. Acceptance: **P2-T9** + spec *`support_quality` aggregation is a pure deterministic rule* / scenario *Aggregation rule table*; Test (**unit-only**, pure transform): enumerated mix table per the scenario.
- [ ] 5.8 Implement quantifier cardinality enforcement against the named `scope`: the seven-way vocabulary (`at-least-one` / `each` / `exactly-one` / `at-most-one` / `none` / `at-least-N` / `at-most-N`). Acceptance: spec *Quantifier cardinality is enforced explicitly* / scenarios *`exactly-one` per-row scope rejects double-publish* / *The ≤-direction family is enforced* / *`scope` binds*; **P2-T15** + **P2-T15b** + **P2-T15c**; Test (**real frontier model**, `N=3, threshold ≥2/3`): per-bound fixtures around their cardinality boundaries; same code vs two `scope` values yields different verdicts.
- [ ] 5.9 Implement `compose` aggregation (all / any / none / implies). `all` rejects on any focal fail; `any` accepts on any focal pass; `none` rejects when any focal *holds*; `implies` accepts vacuously when antecedent false, else reduces to `all` over consequents. Acceptance: spec *`compose` aggregation combines per-triple verdicts correctly* / scenario *All four compose modes agree with their truth tables*; **P2-T16**; Test (zero-model via double): driver feeds per-triple verdicts; assert intent-level `decision` matches the truth table per mode (incl. `implies` antecedent-true and antecedent-false paths).
- [ ] 5.10 Assemble the final `Verdict` per App. A.4: per-triple `{triple_id, focal_verdict, support_quality, polarity, evidence: {focal_claim?, support_claims[] {file, lines, quote, support_triple, triple_verdict}, support_pass_count?}, rationale}` + intent-level `decision` + `aggregate_rationale` + `implies_antecedent_held?` for `implies`. Default verbosity summarizes passing supports as `support_pass_count` and enumerates only failed/low-confidence supports. Acceptance: spec *The Verifier procedure runs the §3.3 steps end-to-end* / scenario *A clean intent on the worked example verifies pass with the full verdict shape* + spec *Passing supports are summarized as a count by default* / scenario *An accurate-supports verdict reports a count, not enumeration*; **P2-T18**; Test (**real frontier model**, `N=3, threshold ≥2/3`): App. B clean verify yields the full-shape Verdict.

## 6. mcp-read-surface  (spec: mcp-read-surface)

- [ ] 6.1 Scaffold `packages/delivery/mcp-server` over the MCP TypeScript SDK; advertise the Phase-2 tools and resources. Implement the `Result`→MCP-success/`DuskError` translator at the boundary; no exception escapes across the MCP boundary. Acceptance: spec *An MCP server hosts the Phase-2 read-only tools, resources, and uniform error envelope* / scenario *The MCP server starts and lists the Phase-2 tools*; Test: spin up the server in-process; advertised tool list contains exactly the named Phase-2 tools; resource list contains the equivalent `dusk://` URIs.
- [ ] 6.2 Implement `dusk_status({})` returning `{active_beads, recent_verdicts, recent_test_runs, index_stats}` with empty collections + populated `index_stats` against an idle server; never returns a `DuskError` for the idle case. Acceptance: **P2-T20** + spec *`dusk_status` returns the documented shape against an idle system* / scenario *Status on a fresh idle server*; Test: idle-server status shape; assert `index_stats` reflects the actual loaded intent / decoration-row count.
- [ ] 6.3 Implement `dusk_inspect({scope})` returning `{intents[], claims[], support_claims[], aspects_unsatisfied[], test_intents[], low_confidence_supports[]}`; hierarchical satisfaction per RFC §2.9; `low_confidence_supports[]` populated from the most recent verdict per intent (D11). Acceptance: spec *`dusk_inspect` reports hierarchical satisfaction, claim lists, and low-confidence supports* / scenarios *Inspect on the App. B fixture mirrors the index truthfully* / *Inspect surfaces low_confidence supports after a low-confidence verdict*; **P2-T11** + the **P2-T8** consumer half; Test: App.B inspect agrees with the derived-index queries; unit-tests child unsatisfied until test code exists; after a low-confidence verdict the support appears under `low_confidence_supports[]`.
- [ ] 6.4 Implement `dusk_verify({diff?, scope?, intents?})` as a thin wrapper over the §verifier-procedure end-to-end. Asserts the call is purely read-side: no fs writes, no commits, no Engineer iteration, no index mutation. Acceptance: spec *`dusk_verify` runs the Verifier procedure read-only and mutates no state* / scenario *Verifying the worked example is non-mutating*; **P2-T12**; Test (**real frontier model**, `N=3, threshold ≥2/3`): App. B clean verify → working tree unchanged, no commit, returned Verdicts include the negative-polarity and `implies` cases with their correct shapes.
- [ ] 6.5 Implement paired list/get tools + MCP resources sharing one query function (D10): `dusk_list_intents`/`dusk_get_intent`/`dusk_list_traces`/`dusk_list_beads`/`dusk_get_bead`/`dusk_list_implement_checkpoints` and the equivalent `dusk://` URIs. Acceptance: spec *Resources and paired tools return structurally equivalent data* / scenario *Resource and paired tool agree on the intents listing*; **P2-T13**; Test: `dusk://intents` read + `dusk_list_intents({})` call → parsed payloads contain the same intent ids and the same per-intent field set (structural equivalence after parse, not byte-identity).
- [ ] 6.6 Wire typed-error envelope on every tool: `dusk_get_intent` against unresolvable → `intent_path_unresolved`; `dusk_verify` with assembled evidence overflow → `verifier_evidence_too_large`; etc. Acceptance: spec *Every tool returns a typed `DuskError` on failure, not a throw* / scenarios *Unresolvable path returns a typed error* / *Evidence overflow returns a typed error, not an exception*; **P2-T14**; Test: both failure paths return the correct kind with `recoverable` set; no exception escapes the MCP boundary.

## 7. dusk-cli-substrate  (spec: dusk-cli-substrate — MODIFIED)

- [ ] 7.1 Add `dusk verify <path>` mirroring the MCP `dusk_verify`. Acceptance: spec *Phase-2 read-path commands* / scenario *`dusk verify` runs the Verifier procedure read-only*; Test (**real frontier model**, `N=3, threshold ≥2/3`): `dusk verify` on App. B prints per-triple verdicts; working tree unchanged; no commit produced.
- [ ] 7.2 Extend `dusk inspect` (the Phase-1 surface) to render hierarchical satisfaction + test-pyramid children satisfaction + the new `low_confidence_supports[]` field. Acceptance: scenario *`dusk inspect` reports hierarchical satisfaction and low-confidence supports*; Test: inspect after a low-confidence verdict shows the new section.
- [ ] 7.3 Add `dusk roles` listing the nine installed role files with each role's declared `memory`, `model`, skill count. Acceptance: scenario *`dusk roles` lists the nine installed role files with their declared scopes*; Test: roles enumeration covers all nine, in a stable order.
- [ ] 7.4 Add `dusk skills` introspecting installed role-bound skills grouped by role. Acceptance: scenario *`dusk skills` introspects the installed role-bound skills*; Test: skills output groups by role matching the `.claude/skills/dusk/<role>/` layout.
- [ ] 7.5 Wire `--help` on every new command — usage description, flag list, ≥1 example. Acceptance: scenario *Every new command supports `--help`*; Test: `dusk verify --help`, `dusk inspect --help`, `dusk roles --help`, `dusk skills --help` each exit 0 with usage substring.

## 8. Canonical artifacts, phase-landing smoke test & cohesive landing

- [ ] 8.1 Extend `packages/intents/canonical/` with three additional canonical intents to cover the new constraint-language surface (the existing six covered Phase-1 / decoration): one **negative-polarity** intent (e.g., `api/no-offset-pagination`), one **`compose: implies`** intent (e.g., `api/idempotency-on-writes` with the `is decorated with api/write-endpoint` antecedent), one **quantifier-bounded** intent (e.g., `notifications/send/publish-sync-per-insert` with `exactly-one` `scope: "per inserted notification row"`). Acceptance: each parses + validates + inspects cleanly; Test: `dusk validate` over all canonical intents is green.
- [ ] 8.2 Author `packages/fixtures/worked-example/` with the App. B `sendNotification` clean-decoration file + the three intents above + a "3-defects" variant that injects (a) one real focal violation, (b) one mismatched `@intent-support` triple, (c) one `api/write-endpoint`-decorated function omitting idempotency-key handling. Acceptance: scaffolding for 8.3.
- [ ] 8.3 **Phase-2 phase-landing smoke test** — "verify the worked example through real MCP + real model." Boot the Dusk MCP server against the worked-example fixture; call `dusk_verify` twice: (a) **clean** → every focal verdict passes; the negative-polarity triple passes (affirmative claim correctly absent); the `implies` antecedent-false intent returns `accept` with `implies_antecedent_held: false` and **no Verifier call for the consequent**; (b) **3-defects variant** → the focal defect fails its `focal_verdict`; the mismatched support surfaces as `support_quality: low_confidence`; the consequent fails with `implies_antecedent_held: true`. Across both passes, a **spawn audit** confirms every Verifier call ran `memory: none` with **no iteration-specific or diagnosis content in its captured `raw_prompt`**. Acceptance: plan Phase-2 phase-landing smoke test (plan lines 367–371); Test (**real frontier model**, `N=3, threshold ≥2/3`): one e2e Vitest scenario exercising the full pass/fail matrix + spawn audit.
- [ ] 8.4 **Verify the Cohesive landing criteria** (archival gate, plan lines 373–379):
  - All P2-T1..T20 (incl. T6b, T6c, T7b, T15b, T15c) green vs real dependencies.
  - The phase-landing smoke test (8.3) passes.
  - `dusk_verify`, `dusk_inspect`, `dusk_status`, the six paired read-only tools, and the equivalent MCP resources operable; `dusk verify --help` / `dusk inspect --help` / `dusk roles --help` / `dusk skills --help` work.
  - No carry-over: the Verifier procedure is complete (no stubbed antecedent path, no flat verdict, no LLM-judged antecedent) and all 9 role files ship with real content including the complete `dusk-verifier.md` template with 4 few-shots.
  - Spawn audit proves `memory: none` freshness and diagnosis non-leakage; dual-channel bead memory exists with mechanical compaction; 20-iter size bound holds.
  - `openspec validate phase-2-runtime-verification --strict` passes.
