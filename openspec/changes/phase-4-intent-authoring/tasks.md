> Ordered by dependency. `dialog-state` is foundational (every later task binds to it); `author-five-stage-flow` is the heart; `author-mcp-surface` wires the runtime to MCP; the three Phase-3 modifications (`bead-decomposition` / `recovery-ladder` / `livelock-detection`) close the stubs at their respective integration points; CLI + MCP-read extensions land last; smoke + cohesive landing gates archival.
>
> Each task names its acceptance (capability spec scenario + plan slug P4-T*) and its Vitest test plan with explicit **determinism surface**. **Control-flow tasks** (state-machine transitions, Stage 4.5 bounces, dialog lifecycle, finalize atomicity, cross-tool pause/resume mechanics) run **zero-LLM via the `ScriptedAuthorResponse` test seam** (Phase-4 analog of Phase 2's scripted-verdict double). **Content-correctness tasks** (Stage 2 grep + classification, Stage 3 greenfield, Stage 4 polarity / antecedent / relates_to emission) run against the **real frontier model at `temperature: 0`** with the pre-registered `N=3, threshold ≥2/3` protocol. **Unit-only** is reserved for pure transforms (the transition function in isolation, `enrichDialogSeed`). Scaffolding/wiring is unmarked.

## 1. Scaffolding & test-harness extension

- [x] 1.1 Scaffold `packages/runtime/author` per `.praxis/features/adding-packages.md`: `type:module`, `private:true`, `exports`, `composite:true`, tsconfig references, colocated Vitest config; wire Turbo `^build`. The package depends on `@dusk/core-schema` (for `DialogState`/`Intent`), `@dusk/core-parser` (for Stage 4.5 primitives), `@dusk/runtime-orchestrator` (for `spawnSubAgent`), and `@dusk/runtime-memory` (for dialog scope). Acceptance: `pnpm build`/`typecheck` pass; dep graph respects core→runtime (wiring — no behavioral test).
- [x] 1.2 Add the Phase-4 frozen seam types to `@dusk/core-schema` (per design D2/D9): `DialogState`, `TranscriptEntry`, `AuthorEntryMode` (`"full" | "scoped_triple_edit" | "l2_recovery"`), the **two new** `DuskError` kinds (`author_finalize_partial_failure`, `author_l2_proposal_unreadable`), and the **rewired** `dusk_resolve_livelock` MCP signature (drop `payload`; add `dialog_init?`). All as Zod schemas with TS via `z.infer`. Acceptance: schemas exported from the core-schema barrel; Phase 5 imports them cleanly; Test: schema validation tests over canonical `DialogState` fixtures including the YAML+Markdown round-trip case (**unit-only** — pure transform).
- [x] 1.3 Extend `@dusk/test-harness` with: (a) **`ScriptedAuthorResponse[]`** + a `scriptedAuthorRuntime(script)` factory implementing the same Author-runtime interface as the real one, returning each scripted response in order with zero LLM calls — the analog of Phase 2's scripted-verdict double; (b) a **`MockDialogStateBuilder`** factory for synthesizing arbitrary `DialogState`s in tests; (c) a **dialog-tail-reader** that loads `.ia/runtime/dialogs/<dialog-id>/state.md` for in-test assertions; (d) a **`MockUnresolvedIntentFixture`** that produces an intent request referencing a missing path (for §5.4 cross-tool integration). Wiring only — exercised by §2–§10.

## 2. dialog-state  (spec: dialog-state)

- [x] 2.1 Implement `DialogState` read/write/destroy at `.ia/runtime/dialogs/<dialog-id>/state.md`. YAML frontmatter + Markdown sections per design D2; atomic write (temp+rename); deterministic round-trip (parse→serialize is byte-identical). Acceptance: spec *The disk format is YAML frontmatter + Markdown transcript with deterministic round-trip* / scenarios *Populated dialog round-trips byte-identically* + *New turn appends without rewriting prior entries*; Test (zero-model + real fs): round-trip a populated fixture; append a new turn and assert prior `## Turn N` bytes unchanged.
- [x] 2.2 Implement `dialog_id` generation using Clock-injected `dlg_<14-digit-yyyymmddhhmmss><3-digit-seq>` (Phase 1 App. D.8 convention). Acceptance: spec *Dialog files live at the documented path with `dlg_…` ids* / scenario *Dialog file is written at the documented path*; Test (zero-model + `MockClock`): generated id matches the regex; sequence numbers increment monotonically within a single Clock-tick.
- [x] 2.3 Implement directory lifecycle: created at `dusk_author_start`, destroyed at `dusk_author_finalize` success, preserved on `dusk_author_finalize` partial failure (per design D8). Acceptance: spec *Dialog files live at the documented path* + spec *Dialog state is persisted on every transition* / scenarios *Mid-dialog harness restart preserves state* + *Multi-turn state accumulates*; Test (zero-model + real fs via scripted-author-runtime): full-flow drives `start → continue × N` → state persists at every turn; finalize success removes the directory; partial-failure preserves it.
- [x] 2.4 Implement per-`dialog_id` advisory file lock for concurrent writes (design D10 risks; design D1 race semantics). Acceptance: spec *Concurrent writes against the same dialog are serialized* / scenarios *Concurrent continue calls serialize without state corruption* + *Continue against a just-finalized dialog returns an unknown error*; Test (zero-model + real fs + concurrent Promise.all): two `dusk_author_continue` calls in flight → serialized; finalize-vs-continue race → loser gets `author_dialog_id_unknown`.
- [x] 2.5 Implement cross-restart state survival. Persistence happens BEFORE the runtime returns the `next_question` to the caller. Acceptance: **P4-T13** + spec *Dialog state is persisted on every transition* / scenario *Mid-dialog harness restart preserves state*; Test (zero-model + real fs): drive 3 turns via scripted-author-runtime; simulate restart (re-instantiate runtime); a 4th `dusk_author_continue` reads `current_stage` and accumulated transcript from disk.
- [x] 2.6 Wire `dialog-state` into Phase-3's existing `dusk doctor --gc-dialogs` (no new CLI work — Phase 3 shipped the command; Phase 4 just makes it reap real dialog state). Acceptance: **P4-T10** (dialog part) + spec *Dialog directories survive 24h then GC* / scenario *Stale dialog is reaped; fresh one preserved*; Test (zero-model + `MockClock` + real fs): create two dialogs (one with `last_touched_at` 30h-aged); `dusk doctor --gc-dialogs` reaps the stale one; the fresh one is preserved.

## 3. author-five-stage-flow  (spec: author-five-stage-flow)

- [x] 3.1 Implement the **pure transition function** `(state, response) → {nextState, outcome}` per design D1. The function is pure (no I/O); the runtime wraps it with LLM calls + persistence. Acceptance: spec *The 5-stage flow is implemented as a pure typed transition function* / scenario *Pure transition is unit-testable against an injected state*; Test (**unit-only**, pure transform): enumerate one Stage-1-advance + one Stage-1-loopback + one Stage-2-tension-pick + one Stage-4.5-bounce input → assert documented `nextState`/`outcome` shapes.
- [x] 3.2 Implement Stage 1 (framing) with the loopback-on-correction branch. Acceptance: **P4-T11** + spec *Stage 1 framing-loopback stays at Stage 1 on user correction* / scenario *A rejected framing regenerates without advancing*; Test (zero-model via scripted-author-runtime): scripted Stage-1 framing + user-rejection → outcome stays at Stage 1; same flow with confirmation → advances to Stage 2.
- [x] 3.3 Implement Stage 2 (discovery via ripgrep over `.ia/intents/` + frontier-model classification per design Q1). Use the existing `@dusk/runtime-orchestrator` `spawnSubAgent` for the classifier call (`tools: [Read, Grep]` advisory). No vector substrate. Acceptance: **P4-T2** + spec *Stage 2 discovery surfaces tensions via agent-driven grep, no vector search* / scenario *Stage 2 finds and classifies an overlap via grep*; Test (**real frontier model**, `N=3, threshold ≥2/3`): with an existing `api/pagination/cursor-only` intent present, author a new cursor-pagination intent → Stage 2 surfaces the existing intent classified `overlap` with resolution options; assert NO embedding/vector substrate code is invoked (trace-stream check for any `embedding`/`vector` events).
- [x] 3.4 Implement Stage 3 (industry-practice injection from training + skill) with the greenfield-on-rejection branch. Use `spawnSubAgent` for the practice-proposal generation; no canonical-library lookup. Acceptance: **P4-T12** + spec *Stage 3 takes the greenfield branch when the practice proposal is rejected* / scenario *Rejected practice proposal yields a greenfield draft*; Test (**real frontier model**, `N=3, threshold ≥2/3`): scripted user rejection of the Stage-3 proposal → advance to Stage 4 with empty practice-scaffold; assert NO canonical-library lookup occurred (no `fetch`/`http`/`packages/intents/canonical/` runtime read events).
- [x] 3.5 Implement Stage 4 (hierarchical drafting + test-pyramid proposal). For implementation intents, propose `…/unit-tests`, `…/integration-tests`, `…/e2e-tests` with canonical `covers-X` triples; the user picks a subset. Acceptance: **P4-T3** + spec *Stage 4 proposes test-pyramid children for an implementation intent* / scenarios *Pyramid children are proposed for an impl intent* + *A pure-leaf utility yields only the picked layer*; Test (zero-model via scripted-author-runtime): scripted Stage-4 proposal + user picks {unit, integration} → `intents_drafted[]` contains exactly impl + 2 children; pure-leaf + {unit} → exactly impl + 1 child.
- [x] 3.6 Implement Stage 4.5 by **importing Phase-1 parser primitives directly** from `@dusk/core-parser`: `validateMatrixPredicateNegation`, `validateAntecedentGrammar`, `validateRelatesToKinds`, `validateAtomicIntent`. Violations produce a bounce-to-Stage-4 outcome with a typed skill-name hint (per design D3). Acceptance: spec *Stage 4.5 imports Phase-1 parser primitives — single source of truth* + scenarios *Matrix-predicate negation bounces with `polarity-decision` hint* + *Behavioral antecedent bounces with `implies-antecedent-grammar` hint* + *`refines` in `relates_to` bounces with `typed-relates-to` hint*; Test (zero-model via scripted-author-runtime injecting each violation): each violation → bounce-to-4 outcome with correct hint; clean draft → advances to Stage 5.
- [x] 3.7 Implement Stage 4 polarity emission: when user framing implies negative meaning, draft affirmative slots + `polarity: negative`. The 4.5 matrix-predicate primitive enforces. Acceptance: **P4-T4** + spec *Stage 4 emits negative meaning as affirmative slots + `polarity: negative`* / scenario *"Must NOT use offset pagination" becomes affirmative + polarity:negative*; Test (**real frontier model**, `N=3, threshold ≥2/3`): user request "list endpoints must not use offset pagination" → drafted triple is affirmative + `polarity: "negative"`; Stage 4.5 passes; Stage 5 commits.
- [x] 3.8 Implement Stage 4 `compose: implies` emission with closed-vocabulary antecedents per RFC §3.2.1. Acceptance: **P4-T5** + spec *Stage 4 emits `compose: implies` with closed-vocabulary antecedents against resolvable references* / scenario *A conditional intent commits with a closed-vocabulary antecedent*; Test (**real frontier model**, `N=3, threshold ≥2/3`): user request "if decorated `api/write-endpoint`, must validate idempotency" → drafted intent has `compose: implies` with `"is decorated with"` antecedent + idempotency consequent; Stage 4.5 passes; Stage 5 commits.
- [x] 3.9 Implement Stage 4 typed `relates_to` emission (5 kinds, never `refines`) + reciprocal-edge proposal for `implies`. Acceptance: **P4-T6** + spec *Stage 4 emits typed `relates_to` with five kinds and proposes reciprocal edges* / scenario *An `implies` edge is emitted and the reciprocal is proposed*; Test (zero-model via scripted-author-runtime): user authors an intent that `implies` an existing one → drafted edge is `{kind: "implies", target}`; the Author proposes a reciprocal edge as the next question; user confirms → reciprocal added; no `refines` ever appears.
- [x] 3.10 Implement Stage 5 atomic finalize across multiple intent files using `@dusk/core-parser`'s atomic write. On any single failure → all rolled back; dialog preserved; `author_finalize_partial_failure` returned (per design D8). Acceptance: **P4-T7** + spec *Stage 5 finalize is atomic across all drafted intent files* / scenarios *Successful finalize writes every drafted intent atomically and resolves them* + *Partial finalize failure rolls back all writes and preserves the dialog*; Test (zero-model + real fs): scripted-author multi-intent dialog → finalize → all files at `.ia/intents/<path>/intent.yaml`; subsequent `dusk_inspect` resolves them; dialog dir removed. Inject a write failure on the second file → all 3 files absent + dialog preserved + correct error.
- [x] 3.11 Author the **seven Author skill files** with concrete authoring guidance (per design Q2): `dusk/author/{polarity-decision, typed-relates-to, implies-antecedent-grammar, tension-detection, discovery-grep-patterns, best-practices-application, test-pyramid-proposal}` under `.claude/skills/dusk/author/`. Each file ≥30 lines of substantive guidance with worked examples (not stubs). Acceptance: spec *Author skills ship with concrete authoring guidance* / scenario *Each skill file is loaded into the Author runtime spawn payload*; Test (zero-model + real fs): every skill file present; each ≥30 substantive lines; Phase-2 `sub-agent-runtime` spawn payload includes all seven; `SubAgentTrace.skills_loaded[]` enumerates them.
- [x] 3.12 Wire the LLM call adapter through Phase-2's `ModelClient` (ambient Claude Code CLI by default). Acceptance: spec *Stage 2 discovery* + spec *Stage 3 industry-practice injection* both depend on a model; Test (zero-model via the `scriptedAuthorRuntime` shim, no production call); the real-model leg is exercised by 3.3 + 3.4 + 3.7 + 3.8 above.

## 4. author-mcp-surface  (spec: author-mcp-surface)

- [x] 4.1 Implement `dusk_author_start({ request, entry_mode?, dialog_init? })` MCP tool. Default `entry_mode: "full"`. Supports `"scoped_triple_edit"` (skips Stages 1–3; Stage 4 with pre-loaded failing triple — for `livelock-detection` rewire) and `"l2_recovery"` (Stage 3 with proposal injected — for `recovery-ladder` L2 wire). Returns `{dialog_id, stage, next_question}`. Acceptance: spec *`dusk_author_start({request})` opens a new dialog and returns the first question* + all three entry-mode scenarios; Test (zero-model via scripted-author-runtime): fresh request → Stage 1; scoped_triple_edit → Stage 4 with `intents_drafted[0]` containing the failing triple; l2_recovery → Stage 3 with the proposal injected.
- [x] 4.2 Implement `dusk_author_continue({ dialog_id, response, payload? })` MCP tool. Loads `DialogState`, runs one transition, persists new state, returns `{stage, next_question}` OR `{finalize_ready: true}`. Acceptance: spec *`dusk_author_continue({dialog_id, response, payload?})` advances the dialog one turn* + scenarios *Continue advances Stage 1 → Stage 2 on framing confirmation* + *Continue loops back to Stage 1 on framing rejection* + *Continue surfaces finalize_ready when Stage 5 is reached*; Test (zero-model via scripted-author-runtime): scripted multi-turn drive → assert each turn's outcome shape and persisted `current_stage`.
- [x] 4.3 Implement `dusk_author_finalize({ dialog_id })` MCP tool. Atomic commits drafted intents per §3.10; success → `{intents_created[]}` + dialog destroyed; partial failure → `author_finalize_partial_failure`. Acceptance: spec *`dusk_author_finalize({dialog_id})` atomically commits the drafted intents and destroys the dialog* + scenarios *Finalize succeeds and returns the created intent paths* + *Finalize against a non-finalize-ready dialog returns a typed error*; Test (zero-model + real fs): scripted dialog → finalize success returns list + dialog deleted; finalize at Stage 2 → `author_stage_invalid_response`.
- [x] 4.4 Wire the typed `DuskError` envelope for the five author kinds: `author_dialog_id_unknown`, `author_stage_invalid_response`, `author_intent_schema_invalid`, `author_finalize_partial_failure`, `author_l2_proposal_unreadable`. No exception escapes the MCP boundary. Acceptance: **P4-T9** + spec *Bad dialog ids return typed errors, not throws* + scenarios *Unknown dialog id returns typed error* + *Invalid stage response returns typed error*; Test (zero-model): each error path returns the correct kind with `recoverable` set + `recovery_hint`.
- [x] 4.5 Implement `/dusk-author` slash command wrapping the 3 MCP tools (`/dusk-author <request>` / `--continue <id> <response>` / `--finalize <id>`). Acceptance: spec *`/dusk-author` slash command is a thin wrapper around the three MCP tools* + all three scenarios; Test (zero-model via scripted-author-runtime): each subcommand prints the documented output and exits 0.

## 5. bead-decomposition (MODIFIED)  (spec: bead-decomposition)

- [x] 5.1 Implement `enrichDialogSeed(unresolvedRefs, snapshot): string` as a pure transform per design D4. Reads each unresolved-ref's surrounding context (parent intent, siblings via path traversal, `relates_to`-linked intents) from the session snapshot. Returns a Stage-1 framing prompt in business-vocabulary terms. Acceptance: MODIFIED spec *`suggested_dialog_seed` content is enriched with surrounding intent context* + scenarios *Missing leaf intent's seed names parent + sibling context* + *Multiple unresolved refs produce a coherent multi-ref seed*; Test (**unit-only**, pure transform over a fixture snapshot): missing-leaf fixture → seed string contains business-vocabulary references to the parent and sibling; multi-ref → seed mentions both refs.
- [x] 5.2 Wire the Phase-3 Decomposer's unresolved-intent escalation path to call `enrichDialogSeed` and populate `ImplementCheckpoint.suggested_dialog_seed` with the result. **`ImplementCheckpoint` shape stays frozen per Phase-3 D4** — only `suggested_dialog_seed`'s string content evolves. Acceptance: MODIFIED spec *`suggested_dialog_seed` content is enriched* scenarios (above); Test (zero-model + real fs): `dusk_implement({request})` against an unresolved-ref fixture → emitted `ImplementCheckpoint` has the enriched seed; the surrounding shape parses against the Phase-3 Zod schema unchanged.
- [x] 5.3 Remove the Sprint-5 escalation stub (the literal `unresolved_refs.join(", ")` fallback) from Phase 3's Decomposer; the `enrichDialogSeed` path is now the only producer of `suggested_dialog_seed`. Acceptance: scaffolding (the removal is verified via grep / no-fallback test); Test: no test path produces the old naive joined-list form post-merge.
- [x] 5.4 **Cross-tool integration test for P4-T8.** Wire the harness side of the recovery: `dusk_implement` returns `implement_paused_for_authoring` → drive the real `dusk_author_start` / `_continue` / `_finalize` flow → `dusk_implement({resume_token})` resumes. Acceptance: **P4-T8** + MODIFIED spec *The Decomposer unresolved-intent escalation path invokes the real Author flow* / scenario *Pause → drive Author → resume completes the pipeline*; Test (**real frontier model + real fs + real git** end-to-end smoke for the cross-tool path; `N=3, threshold ≥2/3`): unresolved-ref request → pause; author the missing intent via the real flow; resume → pipeline completes through Steps 1–9; checkpoint deleted on Step-1 transition (Phase-3 single-use semantic preserved).

## 6. recovery-ladder (MODIFIED)  (spec: recovery-ladder)

- [x] 6.1 Implement the L2 entry-mode dispatch in the Author runtime. When `dusk_author_start({entry_mode: "l2_recovery", dialog_init: {proposal_path}})` is called: read the proposal file; parse against the Phase-3 `intent-proposal.yaml` Zod schema (which lives in `@dusk/core-schema`); inject the proposal's rephrasings as the Stage-3 practice-proposal content; produce an initial `DialogState` at Stage 3 with that injection. Acceptance: MODIFIED spec *Level 2's recovery action consumes `intent-proposal.yaml` via Author Stage 3 injection*; Test (zero-model via scripted-author-runtime + real fs): synthetic L2 fixture with `intent-proposal.yaml` → `dusk_author_start({entry_mode: "l2_recovery"})` opens at Stage 3 with the proposal visible to the user.
- [x] 6.2 Implement the `author_l2_proposal_unreadable` error path: malformed or missing proposal file → typed error without creating a dialog. Acceptance: MODIFIED spec *Level 2's recovery action consumes `intent-proposal.yaml` via Author Stage 3 injection* / scenario *An L2-recovery dialog with a malformed proposal returns a typed error*; Test (zero-model + real fs): missing proposal file → `author_l2_proposal_unreadable`; malformed YAML → same error; no dialog directory created.
- [x] 6.3 **Smoke variant B end-to-end** for the L2 recovery loop. Drive `dusk_implement` to L2 exhaustion (zero-satisfiable bead via scripted-verdict double) → `bead_intent_revision_needed` returned with `intent-proposal.yaml` written → user drives `dusk_author_start({entry_mode: "l2_recovery"})` → `_continue × N` → `_finalize` → refined intent written → user re-invokes `dusk_implement({request: <original>})` → bead completes. Acceptance: spec scenario *L2 error → user authors revision → re-invoke `dusk_implement` succeeds*; Test (zero-model via scripted-verdict double for the bead + scripted-author-runtime for the dialog + real fs/git): full loop closes; the refined intent commits to `.ia/intents/<path>/intent.yaml`; the second `dusk_implement` run produces a normal commit.

## 7. livelock-detection (MODIFIED)  (spec: livelock-detection)

- [x] 7.1 **Hard-remove** the `payload` parameter from the `dusk_resolve_livelock` MCP signature in the schema (Phase 4 D5 cutover). Callers passing the inline-payload Phase-3 form SHALL receive `DuskError { kind: "config_invalid" }` naming the removed parameter and pointing at the new `dialog_init` flow. Acceptance: MODIFIED spec *`dusk_resolve_livelock({verb: "modify_triple"})` opens a scoped Author dialog* / scenario *Inline payload form is rejected post-Phase-4*; Test (zero-model): Phase-3-form call → `config_invalid` with the documented hint.
- [x] 7.2 For `verb: "modify_triple"`, invoke `dusk_author_start({entry_mode: "scoped_triple_edit", dialog_init: {failing_triple: report.failing_triple}})` from `TestVerifierLivelockReport.failing_triple` (the Phase-3 frozen contract). Return the new `dialog_id` so the harness can drive `dusk_author_continue` / `dusk_author_finalize` against it. Acceptance: MODIFIED spec scenarios *`modify_triple` opens a scoped triple-edit dialog*; Test (zero-model via scripted-author-runtime): paused-livelock bead + `dusk_resolve_livelock({verb: "modify_triple"})` → new dialog at Stage 4 with the failing triple pre-loaded.
- [x] 7.3 Implement in-place writeback at Stage 5 finalize for `entry_mode: "scoped_triple_edit"`: the edited triple replaces the previously-failing triple in the existing intent file at `.ia/intents/<test-intent-path>/intent.yaml`. NO new intent file is created. Acceptance: MODIFIED spec scenario *Finalize writes the edited triple in-place and resumes the bead*; Test (zero-model via scripted-author-runtime + real fs): edit a failing triple via scripted-author drive → finalize → the intent file's failing triple is replaced; no new intent file created.
- [x] 7.4 After `dusk_author_finalize`, resume the paused bead at the previously-failing iteration with the refreshed intent visible to the next Verifier spawn (Phase-3 livelock-resume semantics preserved with the refreshed-intent extension). Acceptance: MODIFIED spec scenario *Finalize writes the edited triple in-place and resumes the bead*; Test (zero-model via scripted-verdict double + scripted-author-runtime): drive into livelock → resolve via `modify_triple` → finalize → bead resumes at the failing iter; next Verifier spawn payload contains the refreshed triple.

## 8. dusk-cli-substrate (extended)  (spec: dusk-cli-substrate)

- [x] 8.1 Implement `dusk author <request>` as the CLI mirror of `dusk_author_start`. Print `dialog_id` + Stage 1 framing in human-readable form. Acceptance: spec *The CLI exposes `dusk author` as a direct-invocation mirror of the MCP author surface* / scenario *`dusk author <request>` opens a dialog and prints the first question*; Test (zero-model via scripted-author-runtime): command output contains `dialog_id` + framing question; exit 0.
- [x] 8.2 Implement `dusk author --continue <dialog_id> <response>` mirroring `dusk_author_continue`. Acceptance: spec scenario *`dusk author --continue` advances the dialog and prints the next question*; Test (zero-model + real fs): pre-existing dialog file → command advances and prints next question; exit 0.
- [x] 8.3 Implement `dusk author --finalize <dialog_id>` mirroring `dusk_author_finalize`. Acceptance: spec scenario *`dusk author --finalize` prints the created intent paths*; Test (zero-model + real fs): finalize-ready dialog → command prints created paths; exit 0.
- [x] 8.4 Wire typed-error path: unknown dialog → readable error + non-zero exit. Acceptance: spec scenario *`dusk author --continue` against an unknown dialog returns a typed error and non-zero exit*; Test (zero-model): unknown id → output contains `author_dialog_id_unknown` + exit non-zero.
- [x] 8.5 Wire `--help` on every author command. Usage + at least one example. Acceptance: spec scenario *Every new author command supports `--help`*; Test: each `--help` exits 0 with usage substring.

## 9. mcp-read-surface (MODIFIED)  (spec: mcp-read-surface)

- [x] 9.1 Implement `dusk://dialogs/active` resource + `dusk_list_dialogs` paired tool returning real outstanding-dialog summaries. Each entry: `{dialog_id, request, current_stage, created_at, last_touched_at}`. Both surfaces SHALL share a single underlying query function over `.ia/runtime/dialogs/` (per Phase-2 design D10 paired-tool/resource pattern). Acceptance: MODIFIED spec *`dusk://dialogs/active` and `dusk_list_dialogs` enumerate outstanding Author dialogs* + scenarios *Outstanding dialogs appear in the listing* + *Idle response is empty* + *Resource and paired tool agree*; Test (zero-model + real fs): create a dialog via scripted-author-runtime → `dusk_list_dialogs` returns one entry; empty `.ia/runtime/dialogs/` → returns `{dialogs: []}`; resource read + tool call → structurally equivalent payloads.

## 10. Canonical fixtures, phase-landing smoke test & cohesive landing

- [x] 10.1 Extend `packages/intents/canonical/` with Phase-4-shaped fixture intents to exercise the authoring surface: an existing `api/pagination/cursor-only` intent (parent + a `cursor-decode` sibling) for Stage 2 tension testing; a `compose: implies` canonical fixture (`api/idempotency-on-writes` with `is decorated with api/write-endpoint` antecedent) for Stage 4 antecedent-emission testing; canonical pyramid children for tension-detection skill validation. Acceptance: each parses + validates + inspects cleanly; Test: `dusk validate` over all canonical intents is green.
- [x] 10.2 Extend `packages/fixtures/worked-example/` with Phase-4-specific fixtures: an **unauthored-intent request** that triggers `implement_paused_for_authoring` (smoke Primary); a **conditional-intent authoring request** that walks through `compose: implies` drafting (smoke Primary's intent set); an **L2-exhaustion bead** zero-satisfiable fixture (smoke Variant B); a **24h-aged dialog + checkpoint** fixture (smoke Variant D via `MockClock`). Acceptance: scaffolding for 10.3.
- [ ] 10.3 **Phase-4 phase-landing smoke test — "author then resume, including a conditional intent"** — four scenarios:
  - **Primary (happy path).** In a temp repo, `dusk_implement({request})` for an unauthored target → `implement_paused_for_authoring` with the enriched `suggested_dialog_seed`; drive `dusk_author_start` / `_continue × N` / `_finalize` through the full 5-stage flow — authoring an impl intent with a **`polarity: negative` triple** + its unit-tests child + a **`compose: implies` conditional intent** with a closed-vocabulary antecedent; confirm framing, resolve a discovered tension, accept a practice proposal, pick the unit-tests layer along the way; finalize → intents committed atomically and immediately resolvable. Resume `dusk_implement({resume_token})` → pipeline completes through Steps 1–9 with a commit; checkpoint deleted. **Determinism surface: real frontier model + real fs + real git, `N=3, threshold ≥2/3` for content emissions (P4-T4 polarity + P4-T5 antecedent + P4-T2 tension); scripted-author-runtime for the control-flow assertions on top.**
  - **Variant A — pause/resume — already covered by §5.4 P4-T8.** Smoke re-asserts the end-to-end.
  - **Variant B — L2 recovery — already covered by §6.3.** Smoke re-asserts the end-to-end.
  - **Variant C — Stage 4.5 rejection bounce.** Inject a matrix-predicate-negation attempt → 4.5 bounces back to Stage 4 → user corrects → finalize succeeds. (Zero-model via scripted-author-runtime.)
  - **Variant D — 24h GC** (closes P4-T10). Create a dialog and a checkpoint, age both past 24h via `MockClock`, run `dusk doctor --gc-dialogs` + `--gc-implement-checkpoints` → both reaped; a fresh (<24h) one is preserved. (Zero-model + real fs.)
  Acceptance: plan Phase-4 phase-landing smoke (plan lines ~692–696); Test: one e2e Vitest scenario per variant in a temp repo, gated as the archival pre-requisite.
- [ ] 10.4 **Verify the Cohesive landing criteria** (archival gate, plan lines ~698–705):
  - All P4-T1..T13 pass — control-flow via the scripted-author-runtime (deterministic, no model); content-correctness legs against the real frontier model at `temperature: 0` with `N=3, threshold ≥2/3`. Real fs, real git (for the cross-tool resume test).
  - The four-scenario smoke matrix (Primary + Variants A, B, C, D) all green.
  - `dusk_author_start` / `_continue` / `_finalize`, `/dusk-author`, `dusk author` CLI mirror, `dusk doctor --gc-dialogs`, `dusk doctor --gc-implement-checkpoints` all operable with working `--help`.
  - No carry-over: the Decomposer escalation invokes the **real** Author flow (the Sprint-5 stub is removed from Phase 3's `bead-decomposition`); the `modify_triple` verb is the **dialog-continuation** form (the Phase-3 inline-payload form rejects); the L2 recovery action consumes `intent-proposal.yaml` via Stage 3 injection.
  - All Stage 4.5 validations bounce real violations against the Phase-1 parser primitives; authored intents are schema-valid v2 and immediately resolvable; no code is modified during authoring.
  - `openspec validate phase-4-intent-authoring --strict` passes.
