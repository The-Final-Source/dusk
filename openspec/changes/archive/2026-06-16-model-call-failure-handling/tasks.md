# Tasks — model-call-failure-handling

> Resolves RFC App. D.33. The classification, the narrow seam catch, and the return-vs-throw are **zero-model** with colocated Vitest (the correctness guarantees, incl. the §8 reproductions as regression tests); `--tools ""` is the one model-facing piece (a rate-improver, not a correctness guarantee). Dependency order: classify-at-source → seam catch → tool-surface → integration.

## 1. Classify a content/limit-shaped exit as non-transport (Facet B.i — D1)

- [x] 1.1 In `runtime/verifier/src/modelClient.ts` `runClaude` (`:107-113`): on a non-zero exit, attempt `JSON.parse(out)`; if it yields `{type:"result", subtype:"error_*"}` (an error/limit subtype), throw an error **tagged** with the subtype (e.g. `Object.assign(new Error(msg), { duskModelExit: subtype })`) instead of the bare `Error`. A non-parseable / no-envelope non-zero exit keeps the existing bare-`Error` throw (genuine transport). Acceptance: `model-call-failure-handling` scenario *error_max_turns is not transport* (throw side). Test (zero-model): a stubbed non-zero close whose stdout is an `error_max_turns` envelope → thrown error carries `duskModelExit:"error_max_turns"`; a non-zero close with non-JSON stdout → bare `Error`, no tag.
- [x] 1.2 In `test-harness/src/transportError.ts` `isTransportError` (`:24-30`): add one guard at the **top** — if the error carries the `duskModelExit` discriminant (a string), return `false`. Update the file doc (`:1-18`) to state that a well-formed result envelope with an error subtype is content/limit-shaped, NOT transport. Acceptance: `model-call-failure-handling` scenarios *error_max_turns is not transport* + *A genuine transport failure is still transport*. Test (zero-model): tagged `error_max_turns` error → `false`; `"claude CLI timed out"` → `true`; `ENOENT` errno → `true`; `SyntaxError` → `true`; an untagged `"claude CLI exited 1: …"` (no envelope) → `true`.
- [x] 1.3 Confirm `withTransportRetry` (`runtime/benchmark/src/transportRetry.ts:24-35`) now rethrows a tagged limit error **without** retrying and **without** raising `TransportLegFailure` (it falls through the `!isTransportError(first)` early-rethrow at `:28`). No code change expected — assert via test. Acceptance: §8 Facet-B reproduction. Test (zero-model): `withTransportRetry(observe)` where `observe` rejects twice with a tagged `error_max_turns` error → it rethrows the single classified error (NOT `TransportLegFailure`); with an untagged `"claude CLI exited 1"` error → still `TransportLegFailure` (genuine transport unchanged).

## 2. Surface, don't crash, at the spawn seam — NARROW (Facet B.ii — D2/D3)

- [x] 2.1 Add a shared predicate `isModelCallFailure(err)` (transport- or limit-shaped): `err?.name === "TransportLegFailure" || typeof err?.duskModelExit === "string" || isTransportError(err)`. **Place it (with `isTransportError`) in the `@dusk/core-schema` LEAF**, re-exported from `@dusk/test-harness` for back-compat — because `@dusk/test-harness` is a **dev-only** dependency of `@dusk/runtime-orchestrator` (the seam consumer at runtime), so the predicate cannot be imported from `test-harness` in production `spawn.ts`; `core-schema` is the leaf everyone depends on at runtime. **Detect `TransportLegFailure` STRUCTURALLY by `name`, NOT `instanceof`** — `TransportLegFailure` is exported from `@dusk/runtime-benchmark`, which `core-schema` must not depend on; the `name` check needs no import (no cycle). This realizes the Lead Architect's "one source of truth for failure classification". Acceptance: predicate present + typed in `@dusk/core-schema` (and re-exported from `@dusk/test-harness`); `spawn.ts` imports it from `@dusk/core-schema` (already a runtime dep — no new edge); unit-tested for each arm (transport message, `name:"TransportLegFailure"`, `duskModelExit` tag) and the negative (a `TypeError` → `false`).
- [x] 2.2 In `runtime/orchestrator/src/spawn.ts`: wrap the `verifierFactory` await (`:146`) and the `taskRunner` await (`:165`) in try/catch. On catch: if `isModelCallFailure(err)` → **verifier branch**: set `verdict = duskError("verifier_model_call_failed", <message incl. subtype/attempts>, { bead_id, … })` (flows through `value.verdict` → the existing `loop.ts:171` handling — which re-wraps it into the routed `verifier_model_call_failed`; do NOT over-claim the `recoverable` flag survives on this path); **else (taskRunner) branch**: `return { success:false, error: duskError("task_tool_call_failed", <message>, { recoverable: true, bead_id, … }) }`. If NOT `isModelCallFailure(err)` → **re-throw** (programming errors propagate loud). Acceptance: `model-call-failure-handling` scenarios *A thrown model-call failure on the reasoning path becomes a returned failure* + *…on the verifier path becomes a returned verdict failure* + *A programming bug is not swallowed*. Test (zero-model): inject a `taskRunner` that throws a tagged `error_max_turns` → `spawnSubAgent` returns `{success:false, task_tool_call_failed}`; inject a `verifierFactory` that throws a `TransportLegFailure` (or a tagged limit error) → returns a `verifier_model_call_failed` verdict; inject a `taskRunner` that throws `new TypeError("boom")` → `spawnSubAgent` re-throws the `TypeError` (does NOT return a model-call-failed result).
- [x] 2.3 In `runtime/short-cycle/src/loop.ts`: make the **bead-orchestrator tick** (`:201-214`) check its spawn result — `const tick = await deps.spawn({ role: "bead-orchestrator", … }); if (!tick.success) return tick;` — so a surfaced model-call failure on the tick is propagated, never silently dropped (re-audit HIGH finding: today the tick's `RuntimeResult` is discarded, so the §2.2 seam fix would otherwise convert its crash into a silent false-success). Acceptance: `model-call-failure-handling` scenario *A surfaced failure on the bead-orchestrator tick is not silently dropped*. Test (zero-model): a scripted spawn that returns `{success:false}` on the bead-orchestrator tick → `runShortCycle` returns that failure (the loop does not continue as if it succeeded).
- [x] 2.4 Trace + assert the end-to-end consequence: a `runShortCycle` whose engineer/verifier/bead-orchestrator spawn returns the surfaced `{success:false}` propagates it (`loop.ts:148/:169/:201-new-check`) and `runImplement` surfaces a legible failure (no uncaught throw, no `exit 1` stack). Acceptance: §8 Facet-B reproduction at the run level. Test (zero-model): a scripted spawn whose model call deterministically fails → the short cycle returns a returned failure; assert no exception escapes `runShortCycle`.

## 3. Remove the tool surface; fix the falsified comment (Facet A — D5, model-facing)

- [x] 3.1 In `runtime/verifier/src/modelClient.ts` (`:138-144`): replace `args.push("--disallowed-tools", ...DISABLED_TOOLS)` with `--tools ""` (zero-tool allowlist); keep `--max-turns 3`. Remove or retire `DISABLED_TOOLS` (`:90`) if now unused (don't leave dead config that drifts). Rewrite the `:138-139` comment: the premise "the model recovers and answers in text" is falsified — state that tools are removed at the request surface (`--tools ""`), the cap is a pure blast-radius backstop, and hitting it is non-fatal + surfaced (D.33). Acceptance: `model-call-failure-handling` scenario *tool_use is removed at the request surface*. Test (zero-model presence check on the constructed args, if reachable): args contain `--tools` with an empty-string value and `--max-turns 3`; no `--disallowed-tools`. Verify the flag spelling + the `""`-disables-all-tools sentinel against `claude --help` and record the CLI version checked (v2.1.153 at authoring); the empty-string element reaches the CLI literally under `spawn` (no shell word-splitting).
- [x] 3.2 Frame this as a rate-improver in code comments + ensure no correctness path references the tool suppression. Acceptance: `model-call-failure-handling` scenario *Correctness does not rest on tool suppression* (covered by the §2 mechanical tests holding independently of tool args).

## 4. Integration + landing

- [x] 4.1 Regression test composing the lot (zero-model): the exact §8 reproduction — a model-call leg that deterministically exits `error_max_turns` (a) is classified non-transport, (b) is not cold-retried to `TransportLegFailure`, (c) is surfaced as a returned `task_tool_call_failed`/`verifier_model_call_failed` the short cycle handles, (d) a `TypeError` on the same path still propagates. Acceptance: the proposal's Verifiability bullets, end-to-end.
- [x] 4.2 `pnpm build` + `pnpm typecheck` + `pnpm test` green across affected packages (`runtime-verifier`, `test-harness`, `runtime-benchmark`, `runtime-orchestrator`, `cli`); confirm `runtime/benchmark/src/realAuditCall.ts` is not regressed; `openspec validate model-call-failure-handling --strict` passes; archive (`/openspec-archive-change model-call-failure-handling`).
