# Tasks — boundary-outcome-handling

> Resolves RFC App. D.34 (governing principle RFC §1.2.1). Every guarantee below is **zero-model** with colocated Vitest (the §-repro forms as regressions, including **both honesty duals**); the only model-facing piece is which raw output the agentic bridge interprets when Dusk's schema is absent, and the asymmetry (agent pushes only toward `no_verdict`/`fail`) keeps both duals model-independent. Dependency order (`^build`): `core/schema` → `verifier` + `test-runner` → `short-cycle`/`long-cycle` → `orchestrator` → `cli`. **Eight fixes + one guard-test** (gap #7 is already mitigated). Keep the D.32 no-marker guard (`run.ts:48-60`) and the D.33 spawn seam (`spawn.ts:152-179`) unchanged.

## 1. core/schema — one classifier, one union, the result schema (D1; R2/R3)

- [x] 1.1 NEW `core/schema/src/boundaryOutcome.ts`: the flat TS union `BoundaryOutcome = {kind:"content"} | {kind:"no_verdict", reason: NoVerdictReason} | {kind:"transport"}` (a TS type, NOT a Zod schema). `NoVerdictReason = z.enum(["empty","unparseable","incomplete","deterministic_limit","tool_infrastructure"])` (the one persisted piece — rides in `DuskError.details`). One classifier gate `classifyBoundary(input: {threw: boolean; error?: unknown; parsedToDuskSchema: boolean; completenessHeld: boolean; timedOut: boolean}): BoundaryOutcome` — pure, no I/O, **takes already-derived facts, never a raw exit code as a branchable field** (R1/R4); delegates thrown-error transport/limit classification to `isTransportError`/`isModelCallFailure`. Re-export from `index.ts`. Test (zero-model): each input shape → the right outcome; a thrown `TypeError` is NOT classified `no_verdict`.
- [x] 1.2 In `duskError.ts`: append `"infrastructure_no_verdict"` to `DUSK_ERROR_KINDS` (with a comment: a boundary returned empty/unparseable/incomplete/tool-infra — recovery axis, never content; the `no_verdict` reason rides in `details`). Add a `noVerdictError(reason, opts)` helper next to `duskError` so the three downgrade sites call one constructor (Rule-of-Three; R2). Note in a comment that `test_runner_command_failed` is **superseded** by `infrastructure_no_verdict` for the Stage-2 boundary. Test: `noVerdictError("empty", {bead_id, step:4})` parses, kind is `infrastructure_no_verdict`, `recoverable:true`, `details.no_verdict_reason==="empty"`.
- [x] 1.3 NEW `core/schema/src/testRunResult.ts`: the **Dusk-own** result schema the project adapter emits and the core reads — `DuskTestRunResultSchema = { schema_version: 1, passed: int≥0, failed: int≥0, not_run: int≥0, completed: boolean, cases: DuskTestCase[] }` where `DuskTestCase = { name, outcome: enum["passed","failed","not_run"], duration_ms≥0 }` (`.strict()`). Re-export. This is the only tool-shaped thing the core may read because it is Dusk's own (R4). Test: valid payloads parse; a missing `completed`/counts fails parse (→ caller treats as `no_verdict`).
- [x] 1.4 Lift `withTransportRetry` + `TransportLegFailure` into `core/schema` (they depend only on `isTransportError`, already here), re-exported from `@dusk/runtime-benchmark`/`@dusk/test-harness` for back-compat — so transport-retry is reachable on the runtime verifier path WITHOUT a `benchmark`/`test-harness` edge (resolves the handoff "transport-retry lives only in benchmark" hole). Verify no package cycle (`core/schema` imports nothing internal). Test: existing `transportRetry` tests pass against the lifted location; a single transport death retries once, a second → `TransportLegFailure`, a tagged limit error is NOT retried.

## 2. verifier — positive completeness check; gap #7 guard (D2/D9; R8)

- [x] 2.1 In `runtime/verifier/src/procedure.ts` (`:120-154`): replace `answer?.affirmative_holds ?? false` (`:124`) and `?? "vague"` (`:133`) with a **positive completeness predicate** `verdictComplete(parsed, toJudge): boolean` — every `triple.id` in `toJudge` has a matching `parsed.triples` entry AND every `supportId(triple.id, i)` has a matching `answer.supports` entry. On incomplete (or a parseable-empty `{triples:[]}` under a non-empty `toJudge`) → return `infrastructure_no_verdict` (reason `incomplete`/`empty`), NOT a fabricated focal verdict and NOT a false-converge. Acceptance: spec scenarios *A degraded empty verdict does not falsely converge* + *A missing support entry is not coerced*. Test (zero-model): `{triples:[]}` with a 2-triple intent → `no_verdict`; an answer missing one support id → `no_verdict`; a complete answer → unchanged content verdict.
- [x] 2.2 Guard the agentic bridge's own parse: a non-parseable/empty bridge response resolves to `no_verdict`, never silently `fail`/`pass`. Test (zero-model): a bridge double returning empty → `no_verdict`.
- [x] 2.3 In `runtime/benchmark/src/testPrepass.ts` `realTestPrepassVerdict` (`:87`): replace `answer?.genuinely_verifies ?? false` (and `?? "no judgment returned"` rationale coercion) with the **same positive completeness predicate** — every covered triple MUST have a matching `parsed.triples` entry; incomplete/degraded ⇒ `no_verdict` (incomplete), NOT a fabricated `genuinely_verifies:false` → focal `fail`. This is the **live Stage-1 test-intent pre-pass on the CLI path** (`isTestIntentPath ⇒ route "prepass"`, `implement.ts:301,395-397`) — the boundary the looping test bead routed through, structurally identical to 2.1 (R8). Keep the existing `:54-63` no-marker pre-empt guard unchanged. Acceptance: spec scenario *A degraded empty verdict does not falsely converge* (test-pre-pass path). Test (zero-model): a degraded/empty pre-pass answer for a 2-triple test intent → `no_verdict`, not a fabricated reject; a complete answer → unchanged verdict.
- [x] 2.4 (Guard-test, gap #7) `runtime/verifier/src/modelClient.ts:178`: assert the existing mitigation holds — a `SyntaxError` from the success-path `JSON.parse` is classified transport (`modelCallError.ts:42`) and surfaced at the spawn seam, never an uncaught crash. Optional one-line locality guard; do NOT gate the change on it. Test (zero-model): a malformed `--output-format json` envelope → `isTransportError` true → surfaced, not thrown out of the run.

## 3. test-runner — spawnSync capture, Dusk-schema floor, decision→reenter, no_verdict kind (D3/D4/D6; R4/R5/R7/R9)

- [x] 3.1 In `runtime/test-runner/src/vitest.ts`: `execFileSync` → `spawnSync` (`:18-19`) capturing `{command, exitCode, stdout, stderr, timedOut}` — a non-zero exit is **data**, never a throw. Guard `parseVitestJson`'s `JSON.parse` (`:23`). Remove the `status!=="passed" ⇒ "failed"` coercion (`:29`): non-run (skipped/todo/pending) is neither pass nor fail. Argv stays config-supplied/opaque (no branching on a tool's vocabulary, R4). Acceptance: spec scenario *A subprocess crash is captured as data*. Test (zero-model): a stubbed non-zero exit returns a data shape (no throw); malformed JSON → `no_verdict`-signaling, not a thrown error.
- [x] 3.2 In `runtime/test-runner/src/verdict.ts`: replace `anyPass && !anyFail` (`:19-30`) with the **Dusk-result-schema floor** reading `DuskTestRunResult`: `failed>0 ⇒ content fail`; `passed>0 ∧ failed==0 ∧ completed ⇒ pass`; `not_run` ≠ fail; schema absent/unparseable OR `completed:false` ⇒ `no_verdict`. Acceptance: spec scenarios *A truncated zero-failure result is not read as a pass* + *A non-run status is not coerced to a failure*. Test (zero-model): `{failed:1}`→fail; `{passed:2,failed:0,completed:true}`→pass; `{passed:0,failed:0,not_run:3}`→`no_verdict`; `{failed:0,completed:false}`→`no_verdict`; absent schema→`no_verdict`.
- [x] 3.3 In `runtime/test-runner/src/run.ts`: (a) Stage-1 pre-pass downgrade (`:76-77`) → `noVerdictError`/`no_verdict` (gap #9), NOT `recoverable:false`; (b) classify a content Stage-2 `decision:"fail"` as the existing `reenter_step4` outcome so the orchestrator routes it through the livelock block (gap #1, D4); (c) add a NEW third `TestRunnerOutcome` kind `{kind:"no_verdict", reason}` for the infra case; (d) keep the D.32 no-marker guard (`:48-60`) **unchanged**. Acceptance: spec scenarios *A Stage-2 fail blocks the commit* + *The empty-verdict failure is not downgraded*. Test (zero-model): Stage-1 empty verdict → `no_verdict` kind; Stage-2 `decision:"fail"` → `reenter_step4`; Stage-2 schema-absent → `no_verdict` kind; missing marker → unchanged `reenter_step4` with the D.32 rationale.

## 4. short-cycle / long-cycle — stop the downgrade, honest confirmation (D5/D8; R6/R7/R10)

- [x] 4.1 In `runtime/short-cycle/src/loop.ts`: stop the `:171-172` downgrade — an empty/`isDuskError` verdict → a NEW `ShortCycleOutcome` kind `{kind:"no_verdict", reason}` (peer to `budget_exhausted`), NOT `err(recoverable:false)`. A content `focal_verdict:"fail"` still re-drafts (unchanged, `:176-178,238-239`). Acceptance: spec scenarios *Repeated empty verifier responses do not re-draft correct code* + *A content focal-verdict fail still re-drafts*. Test (zero-model): scripted verifier double returns empty → `no_verdict` outcome, no re-draft, no content-iteration consumed; returns a content fail → re-draft as before.
- [x] 4.2 In `runtime/long-cycle/src/longCycle.ts`: stop the `:35-37` downgrade → `no_verdict`; in the N=2 confirmation (`:62-102`), compute `confirmingRejects` over parsed verdicts only and route a `no_verdict` confirmation to the infra axis — never let the tally fall to 0 and dismiss as flaky. Preserve the GOOD flaky-dismiss otherwise. Acceptance: spec scenario *A reject followed by no_verdict confirmations is not dismissed as flaky*. Test (zero-model): original `reject` + two `no_verdict` confirmations → NOT clean/dismiss → infra axis; original `reject` + two `accept` confirmations → flaky-dismissed (unchanged).

## 5. livelock-detection — the no_verdict precedence arm (D7; R10)

- [x] 5.1 In `runtime/livelock-detection/src/precedence.ts` (`:16-23`): add a `no_verdict` arm to `TickOutcome` and a `noVerdictExhausted: boolean` input; order `if (livelockReport) … ; if (noVerdictExhausted) return {kind:"no_verdict"}; if (budgetExhausted) …`. Keep the module a **pure detector** (the counter lives in the orchestrator; passed in). Acceptance: spec scenario *Infrastructure noise does not trip the livelock detector, but a real livelock still does* (precedence half). Test (zero-model): `{livelockReport, noVerdictExhausted:true}` → livelock (precedence); `{livelockReport:null, noVerdictExhausted:true, budgetExhausted:true}` → `no_verdict` (beats budget).

## 6. orchestrator — consume decision, infra axis + finite counter + resume + defensive catch (D4/D5/D6/D7; R7/R7a/R9/R10)

- [x] 6.1 In `runtime/orchestrator/src/activeRun.ts`: append `"paused_infrastructure"` to `BEAD_STATUSES`.
- [x] 6.2 In `runtime/orchestrator/src/stateMachine.ts` Step-6 loop (`:320-350`): consume the Test Runner outcome — `reenter_step4` (now also carrying a genuine Stage-2 `fail`) routes through the EXISTING livelock-observation block (`:334-347`) with NO new branch (gap #1, D4); a `no_verdict` `TestRunnerOutcome` increments the finite infra counter, NOT `livelockObservations` (only a content reject becomes an observation; consecutiveness over the content sequence). Acceptance: spec scenarios *A Stage-2 fail blocks the commit* + *Infrastructure noise does not trip the livelock detector*. Test (zero-model): Stage-2 `fail` → re-enter Step 4, `commitBead` NOT called; interleaved `no_verdict` → counter increments, detector does not fire; 3 consecutive content rejects → livelock.
- [x] 6.3 Route the short-cycle `no_verdict` outcome and the Stage-2 `no_verdict` to the infra axis: a finite `noVerdictCount` in `processBead`'s closure (config ceiling, mirror `lifetimeMax` sourcing); call `resolveTickPrecedence` with `noVerdictExhausted`; on exhaustion `setBeadStatus(…, "paused_infrastructure", …)`, write the L3 freeze resume record (reuse the recovery-ladder `freezeResume` path), and return an `infrastructure_no_verdict` DuskError with a legible `recovery_hint`. Acceptance: spec scenario *Exhausting the finite counter pauses legibly and resumably*. Test (zero-model): N consecutive `no_verdict` → `paused_infrastructure` + resume record written; below N → continues.
- [x] 6.4 Add a defensive `catch` to the `try…finally` (`:144,213`): a sync throw in `processBead` is surfaced as a legible `DuskError` (`internal_error`) — NEVER an `exit 1`, NEVER relabelled as `no_verdict`/content (honest, D6). Acceptance: spec scenario *A programming bug still fails loud*. Test (zero-model): a thrown `TypeError` in a stubbed step → surfaced `internal_error`, run does not crash, NOT a `no_verdict`.
- [x] 6.5 Confirm `resumeFrozenBead` (`:401`) re-enters `processBead` from the frozen iteration for a `paused_infrastructure` bead (it reads the same freeze record). Test (zero-model): a written infra-freeze record → `resumeFrozenBead` resumes the pipeline.

## 7. cli — spawnSync runner, fold retries into the classifier, accept the new status (D6/D9)

- [x] 7.1 In `cli/src/implement.ts` vitest runner (`:434-444`): `execFileSync` → `spawnSync` (non-zero is data). Test/typecheck only (no new unit needed beyond 3.1's runner contract).
- [x] 7.2 Fold the two ad-hoc retry checks (`:421`, `:428` — `if (!result.success && result.error.kind === "verifier_model_call_failed") …`) into the single classifier so retry/`no_verdict` classification is not a second vocabulary (R2). Acceptance: design D9. Test (typecheck + zero-model where reachable): a `no_verdict` from the verifier factory routes via the classifier, not the ad-hoc kind check.
- [x] 7.3 Accept `paused_infrastructure` in the CLI `IN_FLIGHT` set and the `bd_` resume dispatch (`:465`) so `dusk implement --resume bd_…` resumes an infra-paused bead. Test (zero-model): resume dispatch accepts a `paused_infrastructure` bead-id.

## 8. Integration + landing

- [x] 8.1 Composite regression (zero-model) — **both honesty duals end-to-end**: (Dual A) a scripted verifier double returning empty/`{triples:[]}` → `no_verdict` axis, no re-draft of correct code, finite pause + resume, no crash, no silent green; (Dual B) a Stage-2 `decision:"fail"` → re-enter Step 4 + blocked commit, and a content `focal_verdict:"fail"` still re-drafts; plus a `TypeError` still propagates loud. Asserts the proposal's Verifiability bullets.
- [x] 8.2 Re-audit grep: confirm no `?? false` / `?? "vague"` / `anyPass && !anyFail` / `status !== "passed"` silence-inference survives on the boundary paths; confirm gap #1's re-entry is wired into the existing livelock block (not a new loop).
- [x] 8.3 `pnpm build` + `pnpm typecheck` + `pnpm test` green across affected packages (`core-schema`, `runtime-verifier`, `runtime-benchmark`, `runtime-test-runner`, `runtime-short-cycle`, `runtime-long-cycle`, `runtime-livelock-detection`, `runtime-orchestrator`, `cli`); `openspec validate boundary-outcome-handling --strict` passes; archive (`/openspec-archive-change boundary-outcome-handling`).
