# Tasks — test-pyramid-routing

> Resolves RFC App. D.32. Mechanical/correctness pieces are **zero-model** with colocated Vitest; the Engineer-guidance piece is model-facing (rate-improver, not a correctness guarantee). Dependency order: shared predicate → routing → fail-loud → gate → guidance → integration.

## 1. One shared test-identity predicate (kills the duplication)

- [x] 1.1 Add/extract a single `isTestIntentPath(intentPath, config)` (or reuse `testPyramidSuffixes(config)` directly) as the one place that answers "is this a test intent?" by suffix. Replace `dusk_inspect`'s own `TEST_SUFFIXES_RE` (`delivery/mcp-server/src/queries.ts`) and align the orchestrator's inline `suffixes.includes(...)` (`runtime/orchestrator/src/stateMachine.ts`) to call it. Acceptance: `test-intent-routing` scenario *Routing source of truth is shared, not duplicated*. Test (zero-model): one predicate; the three consumers (verifier, orchestrator, inspect) resolve identical test-identity for a fixture set; no divergent copy remains. **NOTE — behavior change, not pure refactor:** `queries.ts`'s `TEST_SUFFIXES_RE` is config-blind and hardcodes 5 suffixes (incl. `contract-tests`/`property-tests`, not the v1 defaults), so `dusk_inspect`'s test-identity changes under default config once routed through `testPyramidSuffixes(config)` — this fixes a latent config-divergence bug. Assert `dusk_inspect` now honors `dusk.config.yml` suffix overrides.

## 2. Route the CLI verifier by suffix (the root fix — D1)

- [x] 2.1 In `cli/src/implement.ts` `verifierFactory` (~`:352-356`), route to the Stage-1 pre-pass when `isTestIntentPath(vctx.intentPath, ctx.config)` is true — NOT when `testDiscovery(...).length > 0`. A test-suffix intent can never reach ordinary `verifyIntent`. Acceptance: `test-intent-routing` scenario *A test-suffix intent routes to the pre-pass regardless of its marker*. Test (zero-model): a `DerivedIndex` with a test-suffix intent whose only claimant is `marker:"intent"` (empty `testDiscovery`) → the verifier selects the pre-pass, never the ordinary path (assert on the route taken, e.g. via a stubbed pre-pass/ordinary pair).

## 3. Fail loud + legible on a missing body (D3)

- [x] 3.0 **Add `test_intent_no_test_marker` to `DUSK_ERROR_KINDS`** (`core/schema/src/duskError.ts`) — `duskError()` takes a closed enum, so §3.1/§3.2 won't typecheck without it. This is a verdict-channel `DuskError` kind, **distinct from** the gate kind `non_test_marker_on_test_intent` (§4.1); both are needed, no collision. Acceptance: kind present + typed.
- [x] 3.1 In `runtime/benchmark/src/testPrepass.ts` (`realTestPrepassVerdict`): insert an explicit pre-check **before** the model `complete()` call (~`:62`) — when `testDiscovery(intentPath)` is empty for a routed test intent, return a specific recoverable `test_intent_no_test_marker` failure naming the intent + expected markers. (Today an empty body is still sent to the model, which *could* return `true` → a silent accept — so the guard must pre-empt the model call, not rely on the verdict.) Acceptance: `test-intent-routing` scenario *Missing body surfaces the real cause*. Test (zero-model): empty-body input → `test_intent_no_test_marker`, recoverable, with **no** model call made.
- [x] 3.2 In the orchestrator Test Runner path (`runtime/test-runner/src/{run.ts,discovery.ts}`): insert the same guard **before** the Stage-2 `runVitest` call (`run.ts`, after `claims` is computed ~`:36`, before the `runVitest` call ~`:73`). Today an empty `testFilesFor` flows to `runVitest([])` → zero files run → a **silent GREEN pass** (no triple judged) — the guard must pre-empt that, returning the explicit `test_intent_no_test_marker` (re-enter Step 4), not a green no-op. Acceptance: `test-intent-routing` scenario *The Test Runner path also fails loud on an empty body*. Test (zero-model): zero-claim resolution → explicit missing-marker signal, never a green Vitest no-op.

## 4. Reverse of gate Check 9 (D4)

- [x] 4.1 Add `non_test_marker_on_test_intent` to `REJECTION_KINDS` (`delivery/pre-tool-use/src/rejections.ts`) and to App. A.8's union (RFC). Update the `rejections.ts` count docstring: **17 → 18 mechanical kinds** (array 18 → 19 entries incl. the fail-safe `hook_internal_error`); the new kind is gate-only and NOT part of the v1 10-check→12-kind matrix (v1 stays 12 — keep the v1/v1.x framing honest, per the App. A.8 v1.x note). Acceptance: kind present + typed; docstring count matches.
- [x] 4.2 Add the reverse check in `delivery/pre-tool-use/src/checks.ts` (the per-record loop in `runChecks` that holds Check 9, ~`:212-217`): if `record.marker ∈ {intent, intent-file}` AND `record.intent_path` ends in a configured suffix → reject `non_test_marker_on_test_intent` with the fix-it message. Scoped exactly: fires only on a focal marker whose `intent_path` IS the test-suffix intent. Acceptance: `pretooluse-gate` scenarios *`@intent` claiming a test-suffix intent is rejected* + *Legitimate non-test decoration in a test file is NOT rejected* + *The correct test marker passes the gate*. Test (zero-model): `@intent app/x/unit-tests` → reject; `@intent-support …` on a test file → pass; `@intent app/x/foo` (non-suffix) in a test file → pass; `@intent-test-file app/x/unit-tests` → pass.

## 5. Teach the Engineer + signal the test bead (D5 — liveness, model-facing)

- [x] 5.1 Extend `ENGINEER_FILE_INSTRUCTION` (`cli/src/implement.ts:187-200`) to name `@intent-test`/`@intent-test-file` and state the rule: a file implementing a test-suffix intent claims it with a test marker, never `@intent`. Add a `dusk/engineer/*` skill (e.g. `test-file-decoration.md`) teaching test-file decoration with a worked example. Acceptance: `test-intent-routing` scenario *Engineer guidance names the test markers*. Test (zero-model presence check): the instruction + a skill name the test markers; ≥1 worked example.
- [x] 5.2 In the orchestrator/CLI per-bead task assembly, when the target intent path ends in a configured suffix, append an explicit "this is a test bead — its file body is the evidence the pre-pass judges" signal to the Engineer task string. Acceptance: `test-intent-routing` scenario *Engineer guidance names the test markers* (per-bead-signal clause). Test (zero-model): a test-suffix bead's assembled task contains the test-bead signal; a non-test bead's does not.

## 6. Integration + landing

- [x] 6.1 End-to-end (zero-model where possible; real-model leg correctness-gated): build/verify a test-suffix intent fixture decorated `@intent` → assert it routes to the pre-pass and fails `test_intent_no_test_marker` (not silent accept); then with `@intent-test-file` → assert the pre-pass judges it (and can reject for genuine test-quality reasons per §3.4, restoring the two-stage guarantee); assert the gate rejects the `@intent`-on-test-suffix case at write time; assert the suffix-based and marker-based consumers agree under the enforced invariant (`testChildrenByLayer` / Stage-2 / inspect consistent). Acceptance: the proposal's Verifiability bullets, end-to-end.
- [x] 6.2 `pnpm build` + `pnpm typecheck` + `pnpm test` green across affected packages; `openspec validate test-pyramid-routing --strict` passes; archive (`/openspec-archive-change test-pyramid-routing`).
